# Voice AI Bot Dependencies

# Core dependencies
python-dotenv
openai
websockets

# Audio processing
pydub
numpy

# SIP Trunking Support (ONLY needed if USE_SIP_TRUNK=true)
# NOTE: PyAudio and pjsua2 require C++ compilers / build tools to install on Windows.
# Comment out if you are only using the default WebSocket mode.
# pjsua2
# PyAudio

# Optional: for better audio quality
# librosa
# soundfile
