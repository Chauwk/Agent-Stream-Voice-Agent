# Voice AI Bot Dependencies - SIP ONLY MODE

# Core dependencies
python-dotenv
openai
asyncio

# Audio processing
pydub
numpy

# SIP Trunking Support (REQUIRED for SIP mode)
pjsua2
PyAudio

# Optional: for better audio quality
# librosa
# soundfile
